# Copy to .env.local (ignored by git) to run the app against the shared server
# on your own machine. Leave both unset and it runs on one browser only.
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
