# Revision 4 — 17 September 2026

Six changes. All built.

| # | Asked for | What changed |
|---|---|---|
| 1 | Record a sale offered Orchid 30ml at RM 188 only; there should be retail, promotion and, where there is one, an offer price | Every item now shows one button per price it can go for. Tap the one the customer paid. The same item at two prices is two lines in the basket, and the takings count each line at its own price. |
| 2 | Close the day cramped and hard to use on a phone | Below tablet width every list on the page is a stack of cards with the box to type in always on screen. Nothing scrolls sideways any more. |
| 3 | Flags on "Where is the customer from?" — the client's PC showed "CN", "IN", "ID" instead | Every flag is now a small picture served by the app itself, so it looks the same on a Windows laptop as on an iPad. See below. |
| 4 | Clearing the quantity box on a top-up line deleted the line | The box keeps what is typed, and a line only leaves the order by its own cross. A line left with no quantity is tinted, counted in the heading as "still to fill in", and stops the closing until it is filled or removed. |
| 5 | The coloured cards on the dashboards were not clickable | Every headline tile now opens the screen behind its number — orders to approve open the orders, products below reorder open stock, and so on — with a small arrow so that is visible. Tiles that stay on their own page (the warehouse's) scroll to their list instead. A tile only links where that role may go. |
| 6 | Sort the demo logins by role | Grouped in the order of the company chart — Director, MD, Operational Manager, PA, Finance, Warehouse, IT — with the store promoters last, by store. |

---

## The price a sale went for

The price list in *CRM Revision 2* gives every line two prices — retail and
promotion — and the three Wishes a third, an RM 10 offer. Until now the counter
recorded a sale at whichever price the store is *counted* on, so a customer who
paid full price at Pavilion was recorded at the promotion price, and a Wish
added on at RM 10 was recorded at RM 88.

Now each item is offered at every price it can go for:

- **Promotion** and **Retail** on everything, the store's own price first —
  promotion nearly everywhere, retail at BSAS and Sasa;
- **Offer** on Wish 1, 2 and 3 only, because only they carry one.

The basket, tonight's closing, the Activity line, and every report count the
line at the price that was tapped. Where nothing was tapped — the seeded
history, and the dealer and consignment channels, which report a figure rather
than ringing up a sale — the store's basis still applies, exactly as before.

An item without an offer price can never be sold at one; asking for a tier the
item does not have falls back to its everyday price.

This is also the first time the question *"how much did we sell at full
price?"* has an answer in the data.

---

## Closing on a phone

The shelf count was a four-column table with a minimum width, so on a phone the
count box sat off the right-hand edge. That was the screenshot the client sent.

The shelf is now a card per product below tablet width — the name, what sold
and what should be there on the left, the box on the right — and the table only
appears where there is room for it. The top-up lines, the product picker and
the closing button are laid out the same way, and the page is checked at 390px
with no sideways scroll.

---

## Flags

The flags were emoji, and **Windows has no flag emoji** — Chrome and Edge there
print the two letters of the country code instead, which is what the client's
screenshot shows. iPhones, iPads and Macs draw them; the laptops at HQ do not.

All 200 flags are now small pictures (`public/flags/`, 80KB in total, public
domain from flagpedia.net) served by the app itself, so nothing at the counter
depends on a third party, and they look the same on every device. They appear
wherever a country does: the sale screen, the country search, the day's log,
the nationality filter on Analytics, and every "which countries bought" chart.

---

## Still open from Revision 2

The e-mail addresses (which the code goes to), which KL store the twelve "KL"
promoters belong to, the promoter name spellings, whether Love, Hope and
Confidence are sold, and whether the twenty-odd dealers dropped from the list
went deliberately. See [`2026-09-09-revision-2.md`](./2026-09-09-revision-2.md).
