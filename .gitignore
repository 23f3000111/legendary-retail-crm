node_modules
dist
.DS_Store
*.local
.vite
.playwright-mcp

# The client's own documents — their org chart, their store list and their
# completed discovery form. They describe how Legendary runs and who works
# there, so they stay out of a public repository. Everything that was decided
# from them is written up in docs/spec/ instead.
docs/client-input/

# Real keys for a real server. Never committed.
.env
.env.*
docs/.env
!.env.example
passwords*.txt
backup-url*.txt
backup-passphrase*.txt
